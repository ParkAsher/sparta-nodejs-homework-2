const express = require("express");
const router = express.Router();

/* middleware */
const authMiddleware = require("../middlewares/AuthMiddleware.js");

/* lib */
const { } = require("../lib/JoiSchema.js");
const { } = require("../lib/CustomError.js");

// 댓글 작성 API


module.exports = router;